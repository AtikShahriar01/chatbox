# npm config — relative paths so the folder works from ANY drive/location.
# (env.sh/env.bat also set these as absolute env vars, which take precedence.)
cache=.npm/cache
prefix=.npm-global
update-notifier=false
fund=false
