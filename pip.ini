# pip config — cache dir comes from PIP_CACHE_DIR (set by env.sh/env.bat),
# so no absolute path lives here and the folder stays portable.
[global]
disable-pip-version-check = true
no-warn-script-location = true
