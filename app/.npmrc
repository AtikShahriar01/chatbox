# npm runs from app/ most of the time — relative to here, caches still land
# in the workspace root's .npm folder (nothing outside this folder).
cache=../.npm/cache
prefix=../.npm-global
update-notifier=false
fund=false
